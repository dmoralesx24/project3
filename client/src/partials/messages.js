import react from "react";



// im
// // todo 
// // use this as an error message for inside the react Register/User page
// {if(typeof errors != "undefined") {
//     {errors.forEach(function() {
//         // you can use bootstrap here ALERT component
//         {error.msg} 
//     })}
// }}

// {if(success_msg != "") {

//         // you can use bootstrap here ALERT component
//         {success_msg} 
// }};

// {if(error_msg != "") {
//         // you can use bootstrap here ALERT component
//         {error_msg} 
// }}

// {if(error != "") {
//     // you can use bootstrap here ALERT component
//     {error} 
// }}


const ErrorHandeling = WrappedComponent => ({ showError, children }) => {

}